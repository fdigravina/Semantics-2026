# Semantics-2026
To run the code, be sure to download the dbpedia_undirected_light.pickle file from the official FactKG repository. The code must be converted into a db using the kg_to_db.py code. During this procedure, an indexed DB will be stored in memory. This DB weights about 20GB.
To run codes using small language models, be sure to download the gguf files from hugging face.
